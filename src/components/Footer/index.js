import { Typography, Box } from '@material-ui/core';

const Footer = () => (
  <Box mt={5}>
    <Typography variant="body2" color="textSecondary" align="center">
      Disciplina de Prática Profissional Integrada I
    </Typography>
  </Box>
);

export default Footer;
